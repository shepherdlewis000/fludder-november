# procfile: processes executed on Heroku app startup
# format: <process type: <command>
# <process type>: web, worker, urgentworker, clock...
# <command>: the command that every dyno of the process type should
# execute (ex: rake jobs:work)
# process types: web server, worker processes, singletons (clock), tasks...


web: node app.js
